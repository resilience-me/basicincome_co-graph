/* this service manages the API calls to Ripple */

var module = angular.module('app');

module.service('$ripple_connection', ['$rootScope', '$network', function($scope, $network)
{
    
  this.submit = function(){
      
  }

}]);